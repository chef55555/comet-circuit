# Comet Circuit

A one-minute browser arcade game. Steer with Arrow keys or A/D, collect cyan energy shards to build an 8× multiplier, and dodge incoming comets before the circuit closes.

## Local development

```bash
npm install
npm run dev
```

## Deployment

Every push to `main` is built and deployed to GitHub Pages by `.github/workflows/deploy-pages.yml`.
